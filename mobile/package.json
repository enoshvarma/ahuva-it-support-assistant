{
  "name": "ahuva-it-support-assistant-mobile",
  "version": "1.7.0",
  "description": "Ahuva IT Support Assistant \u2014 Android mobile app",
  "private": true,
  "scripts": {
    "build:web": "node build-web.js",
    "test:ui": "node test/ui-smoke.js",
    "cap:sync": "npx cap sync android",
    "build:android": "npm run build:web && npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/preferences": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
